# Cloudflare Pages automatically handles SPA routing
# It serves existing files and falls back to index.html for 404s
# No explicit rules needed - this file can be empty or contain only specific redirects
